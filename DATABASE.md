## Database

The project uses SQLite.

The database file `fstbank.db` is provided in the file `database/`.

To run the application :
1. Copie `database/fstbank.db`
2. Paste the file into `WILDFLY_HOME/standalone/data/`
3. start up WildFly